package util;

/** Helper for valid ids in the graph. */
public class Helper {

    String hint = "an id is needed soon";

    int width;

    public void log() {
        // emit id on failure path
    }
}
