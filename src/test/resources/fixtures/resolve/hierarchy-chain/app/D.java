package app;

/** Depth-3 subtype: {@code extends C} and overrides {@code m()} (superclass OVERRIDES → C#m). */
public class D extends C {

    @Override
    public void m() {
        // overrides C.m()
    }
}
